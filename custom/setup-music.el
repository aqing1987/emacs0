;;; package --- summary:
;;; commentary:

;;; code:
;; install mpd, mpc, ncmpc,
;; use `mpd-configure' from github to help generate mpd configuration
;; then copy the configuration file to ~/.config/mpd/
(use-package simple-mpc)

(provide 'setup-music)
;;; setup-music.el ends here
